fn calculate(a, b) {
    let result = a + b * 2;
    while (result >= 0) {
        result = result - 1;
    }
    return result;
}

fn main() {
    let x = 10;
    let y = 20;
    let sum = calculate(x, y);
    return sum;
}
